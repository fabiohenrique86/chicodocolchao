@{
    ViewBag.Title = "NFe - Entrada";
}
<div class="container" style="padding-bottom: 20px;">
    <h2><i class="glyphicon glyphicon-triangle-right" style="color: #0f3456" title="Nota Fiscal"></i> Entrada de NFe</h2>
    <hr />
    <form id="NotaFiscalForm" name="NotaFiscalForm" method="post" action="ImportarXML" enctype="multipart/form-data">
        <p>
            Lembre-se, <b style="color: red">TODAS</b> as notas fiscais importadas irão para o estoque da loja do <b>DEPÓSITO</b>.
        </p>
        <p>
            Selecione os arquivos de Nota Fiscal Eletrônica (NF-e)
        </p>
        <div class="form-group">
            <input name="arquivos" type="file" accept="text/xml" multiple />
        </div>
        <div class="form-group">
            <button id="btnImportar" type="submit" class="btn btn-success">Importar</button>
        </div>
        @if (ViewBag.Erro != null)
        {
            if (ViewBag.Erro)
            {
                <p style="color: red">
                    @ViewBag.Mensagem
                </p>
            }
            else
            {
                <p style="color: #5cb85c">
                    @ViewBag.Mensagem
                </p>
            }
        }

    </form>
</div>